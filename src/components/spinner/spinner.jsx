import { StyledSpinner, StyledBackdrop } from './spinner.styles';

const Spinner = () => {
  return (
    <StyledBackdrop>
      <StyledSpinner />
    </StyledBackdrop>
  );
};

export default Spinner;
